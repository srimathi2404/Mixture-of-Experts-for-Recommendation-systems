# --- PyTorch ---
# Install this FIRST and separately, matching your CUDA version.
# Locally (no GPU, just for writing/testing code):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# On the server (check `nvidia-smi` for CUDA version, then use the matching
# command from https://pytorch.org/get-started/locally/), e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Do NOT just `pip install -r requirements.txt` for torch — the CPU/GPU build
# has to match the machine it's running on, local vs. server will differ.
torch>=2.1.0

# --- Core ---
recbole>=1.2.0
numpy<2.0
pandas==2.1.4
pyyaml>=6.0

# --- Viz / analysis (attention plots, gate-utilization plots) ---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Utilities ---
tqdm>=4.65.0
scikit-learn>=1.3.0
python-dotenv>=1.0.0

# --- Experiment tracking ---
tensorboard>=2.14.0